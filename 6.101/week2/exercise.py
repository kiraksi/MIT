# Class Exercise

def deriv(f, dx=0.001):
    """
    Given a function f of one variable (which takes a float value and
    returns a float value), returns a function that approximates the
    derivative df/dx.

    Optional parameter dx > 0 is the width of the approximation 
    (the true derivative is the limit as dx -> 0).

    """
    pass

